import React from 'react';
import img404 from '../images/404.png';

const NotFound = (props) => {
    return (
        <div>
            <img src={img404} alt=''/>
        </div>
    );
};

export default NotFound;